
import { DercEntry } from './types';

/**
 * Se han eliminado los ejemplos para iniciar la aplicación con una base de datos limpia.
 * Los nuevos registros se guardarán automáticamente en la memoria del navegador (IndexedDB).
 */
export const MOCK_DERCS: DercEntry[] = [];
